node_modules/
.env
.env.local
bun.lockb
# agent tooling (daisyUI loaded via CDN at runtime, skill files not needed by app)
.agents/
skills-lock.json
# firmware lives in its own repo (JungyoKim/Drone-Gateway)
firmware/
# public/ is now Vite build output (web/ -> `bun run build` -> public/),
# produced by the Dockerfile's build stage. Not source, not committed.
public/
